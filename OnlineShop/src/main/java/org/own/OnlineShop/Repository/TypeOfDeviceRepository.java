package org.own.OnlineShop.Repository;

import org.own.OnlineShop.model.TypeOfDevice;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TypeOfDeviceRepository extends JpaRepository<TypeOfDevice, Short> {
}
