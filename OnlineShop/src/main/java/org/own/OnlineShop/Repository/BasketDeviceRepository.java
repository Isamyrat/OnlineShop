package org.own.OnlineShop.Repository;

import org.own.OnlineShop.model.BasketDevice;
import org.springframework.data.repository.CrudRepository;

public interface BasketDeviceRepository extends CrudRepository<BasketDevice, Long> {
}
